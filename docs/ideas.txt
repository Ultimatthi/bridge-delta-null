# general:
- replace "_elements" with "_list"
- remove unused functions and variables
- use PEP8 format style
- replace northsouth/eastwest with we/they

# drawing:
- fix light texture effect on beige color

# connection
- fix data transfer when rejoining
- remove silently disconnected clients

# lobby
- screen resolution widget
- "save as default" button
- do not trigger "ESC" with active modifier "CTRL"

# scoring:
- show scoring report

# get available attributes of an object:
- print("Available attributes:", [attr for attr in dir(self.object)])

# GitHub
- add visual previews